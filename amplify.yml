version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
        - nvm install 16
        - nvm use 16
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
